```
  ___  _  ______ ___ 
 / _ \| |/_/ __ `__ \
/  __/>  </ / / / / /
\___/_/|_/_/ /_/ /_/ 
                     
```

Use the EXM_HOME environment variable to set the path to the libexm.so library.
Quick example:

```
make
EXM_HOME=$(pwd) ./exm <program> [program arguments]
```

Or install into the default location of /usr/local/:

```
make
sudo make install
exm <program>
```

Optionally `make CFLAGS=-DDEBUG2` to enabled basic log messages to stderr.
Optionally `make CFLAGS=-DDEBUG` to enabled verbose log messages to stderr.


# API Documentation

